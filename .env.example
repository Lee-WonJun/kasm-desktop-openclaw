OPENCLAW_GATEWAY_TOKEN=changeme-replace-with-random-token
VNC_PW=password

# Gateway bind mode: loopback (default, container-only) or lan (expose to host)
# If lan, also add "18789:18789" to ports in docker-compose.yml
# OPENCLAW_BIND=lan
